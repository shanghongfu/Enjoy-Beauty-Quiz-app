// ============================================================
// Bunny Stream configuration
// Fill these two values from your Bunny.net Stream library.
//
// Where to find them (Bunny.net Dashboard → Stream → your library):
//   • Library ID : "Library ID"  (a number, e.g. 123456)
//   • API Key    : "Login / FTP / API" → "API Key"  (library-scoped key)
//
// SECURITY: this is a library-level key (not your account key). To keep it
// safe, in the same library settings set "Allowed Referrer" to your site
// domain (shanghongfu.github.io) so only your site can upload/delete.
// The TUS upload signature is computed in the browser via Web Crypto, so the
// key never has to leave the client.
// ============================================================
window.BUNNY_LIBRARY_ID = '749980';   // Library ID
window.BUNNY_API_KEY    = 'ef83a08b-2ffd-45ea-86289da56fe9-8ac5-46e4';  // library-scoped API Key
